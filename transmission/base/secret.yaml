apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: transmission-creds
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: transmission-creds
    template:
      type: Opaque
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: secret/db3000/transmission/ui
        property: username
    - secretKey: password
      remoteRef:
        key: secret/db3000/transmission/ui
        property: password
